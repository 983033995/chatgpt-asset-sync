PORT=8787

# GitHub credential used by the sync service. For production, prefer GitHub App/OAuth.
GITHUB_TOKEN=

# Optional defaults. They can be overridden at runtime with set_asset_repository.
ASSET_REPOSITORY=983033995/openmontage-assets
ASSET_BRANCH=main
ASSET_BASE_PATH=projects

# Development config store path.
CONFIG_STORE_PATH=./data/configs.json
